# MemoMind Environment Configuration
# Copy to .env and modify as needed

# REST API port
MEMOMIND_PORT=8000

# MCP Server HTTP port
MEMOMIND_MCP_PORT=8001

# Optional API key for REST API authentication (leave empty for no auth)
MEMOMIND_API_KEY=
