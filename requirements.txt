# MemoMind 核心依赖
# Python 3.10+

# 数据库（内置）
# sqlite3 — Python 内置

# 测试
pytest>=7.0.0
httpx>=0.24.0

# 中文分词
jieba>=0.42.1

# REST API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# 导出
pyyaml>=6.0.0

# 性能基准
tabulate>=0.9.0
